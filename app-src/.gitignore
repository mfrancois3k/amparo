# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Convex CLI local state (auth/cache) -- machine-specific, never shared.
# convex/_generated/ is intentionally NOT ignored: it's regenerated types,
# committed so collaborators/CI type-check without running `convex dev` first.
.convex

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
