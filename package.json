{
  "name": "amparo-api",
  "private": true,
  "description": "Dependencies for Vercel serverless functions under /api. Not the app itself \u2014 app-src/package.json builds /app; root index.html has no build step.",
  "dependencies": {
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vercel/node": "^5.10.0",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "test": "node --test tools/jurisdictions/*.test.mts tools/lib/*.test.mts tools/*.test.mts app-src/convex/lib/*.test.mts app-src/src/**/*.test.mts",
    "build:jurisdictions": "node tools/build-jurisdictions.mjs",
    "check:jurisdictions": "node tools/build-jurisdictions.mjs --check"
  }
}
