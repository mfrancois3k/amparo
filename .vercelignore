# Keep internal working docs out of the public deploy.
#
# The repo root IS the deploy root (static, no build step), so anything tracked
# here is served at amparohq.com/<path> unless excluded. These directories hold
# strategy notes, funnel numbers, unresolved legal-exposure notes, and battle
# plans — none of it is site content and none of it is linked from any route.
#
# Verified before excluding (2026-08-20): no href/src in index.html, app/ or
# arena/ references any of these, and none appear as a path in sitemap.xml.
# Live routes are: /, /about, /acerca, /como-verificamos, /derechos,
# /how-we-verify, /rights, plus /app and /arena. Since 2026-09-02 also /pack
# (the app page), /rehearse (rewrite onto /arena/), /aid (rewrite onto
# /new/aid.html) and the generated /data/*.json.

notebook/
tasks/
wargames/

# Same family — the wargame system's ledger and grading rubric, at repo root.
LEDGER.md
SUCCESS.md

# 2026-09-03 blind-spot audit: research notes, backend source, agent config and
# build folders were all served 200 on the domain (/research/RESEARCH-STATUS.md,
# /app-src/convex/http.ts, /.mcp.json, /.claude/launch.json, 72 MB of
# scrollcraft builds). None is site content. Served surfaces stay: pack.html,
# new/, arena/, app/, rights/, derechos/, about/, acerca/, how-we-verify/,
# como-verificamos/, img/, audio/, cards/, og/, data/, brand assets the pages link.
.claude/
.agents/
.focus-group/
.github/
.mcp.json
app-src/
research/
tools/
scrollcraft/
docs/
growth/
node_modules/
*.md
